--// Services
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local ESPEnabled = false
local TeamCheckEnabled = false
local AimbotEnabled = false
local AimSmoothing = 0 -- Controls aim smoothing
local AimKey = Enum.UserInputType.MouseButton2 -- Default to right-click

--// Variables for ESP
local espObjects = {}

--// GUI Setup
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game.CoreGui

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 300)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

-- Title
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Text = "Aimbot and ESP Settings"
TitleLabel.Size = UDim2.new(1, 0, 0, 30)
TitleLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextScaled = true
TitleLabel.Parent = MainFrame

-- Toggle Aimbot Button
local ToggleAimbotButton = Instance.new("TextButton")
ToggleAimbotButton.Text = "Toggle Aimbot: OFF"
ToggleAimbotButton.Size = UDim2.new(1, -10, 0, 30)
ToggleAimbotButton.Position = UDim2.new(0, 5, 0, 40)
ToggleAimbotButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ToggleAimbotButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleAimbotButton.Parent = MainFrame

-- Toggle ESP Button
local ToggleESPButton = Instance.new("TextButton")
ToggleESPButton.Text = "Toggle ESP: OFF"
ToggleESPButton.Size = UDim2.new(1, -10, 0, 30)
ToggleESPButton.Position = UDim2.new(0, 5, 0, 80)
ToggleESPButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ToggleESPButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleESPButton.Parent = MainFrame

-- Toggle Team Check Button
local ToggleTeamCheckButton = Instance.new("TextButton")
ToggleTeamCheckButton.Text = "Toggle Team Check: OFF"
ToggleTeamCheckButton.Size = UDim2.new(1, -10, 0, 30)
ToggleTeamCheckButton.Position = UDim2.new(0, 5, 0, 120)
ToggleTeamCheckButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ToggleTeamCheckButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleTeamCheckButton.Parent = MainFrame

-- Set Aim Key Button
local SetAimKeyButton = Instance.new("TextButton")
SetAimKeyButton.Text = "Set Aim Key: Right Click"
SetAimKeyButton.Size = UDim2.new(1, -10, 0, 30)
SetAimKeyButton.Position = UDim2.new(0, 5, 0, 160)
SetAimKeyButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
SetAimKeyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SetAimKeyButton.Parent = MainFrame

-- Smoothing Slider
local SmoothingLabel = Instance.new("TextLabel")
SmoothingLabel.Text = "Smoothing: 0"
SmoothingLabel.Size = UDim2.new(1, -10, 0, 20)
SmoothingLabel.Position = UDim2.new(0, 5, 0, 190)
SmoothingLabel.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
SmoothingLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
SmoothingLabel.TextScaled = true
SmoothingLabel.Parent = MainFrame

-- Slider for Smoothing Adjustment
local SmoothingSlider = Instance.new("Frame")
SmoothingSlider.Size = UDim2.new(1, -10, 0, 10)
SmoothingSlider.Position = UDim2.new(0, 5, 0, 220)
SmoothingSlider.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
SmoothingSlider.Parent = MainFrame

local SliderBar = Instance.new("Frame")
SliderBar.Size = UDim2.new(0, 10, 1, 0)
SliderBar.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
SliderBar.Parent = SmoothingSlider

-- Dragging Logic for Smoothing Slider
local isDragging = false
SmoothingSlider.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        isDragging = true
    end
end)

SmoothingSlider.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        isDragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if isDragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local mouseX = input.Position.X
        local sliderStart = SmoothingSlider.AbsolutePosition.X
        local sliderEnd = sliderStart + SmoothingSlider.AbsoluteSize.X
        local relativePosition = math.clamp(mouseX - sliderStart, 0, SmoothingSlider.AbsoluteSize.X)
        SliderBar.Size = UDim2.new(0, relativePosition, 1, 0)
        
        -- Update AimSmoothing based on slider position (0-10)
        AimSmoothing = math.floor((relativePosition / SmoothingSlider.AbsoluteSize.X) * 10)
        SmoothingLabel.Text = "Smoothing: " .. AimSmoothing
    end
end)

-- Toggle GUI visibility with Right Shift
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.RightShift then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

-- Toggle Aimbot Function
ToggleAimbotButton.MouseButton1Click:Connect(function()
    AimbotEnabled = not AimbotEnabled
    ToggleAimbotButton.Text = "Toggle Aimbot: " .. (AimbotEnabled and "ON" or "OFF")
end)

-- Toggle ESP Function
ToggleESPButton.MouseButton1Click:Connect(function()
    ESPEnabled = not ESPEnabled
    ToggleESPButton.Text = "Toggle ESP: " .. (ESPEnabled and "ON" or "OFF")
end)

-- Toggle Team Check Function
ToggleTeamCheckButton.MouseButton1Click:Connect(function()
    TeamCheckEnabled = not TeamCheckEnabled
    ToggleTeamCheckButton.Text = "Toggle Team Check: " .. (TeamCheckEnabled and "ON" or "OFF")
end)

-- Set Aim Key Function
SetAimKeyButton.MouseButton1Click:Connect(function()
    SetAimKeyButton.Text = "Press New Aim Key"
    local connection
    connection = UserInputService.InputBegan:Connect(function(input)
        if input.UserInputType ~= Enum.UserInputType.MouseMovement then
            AimKey = input.UserInputType
            SetAimKeyButton.Text = "Set Aim Key: " .. tostring(AimKey)
            connection:Disconnect()
        end
    end)
end)

-- Aimbot Function
local function Aimbot()
    if AimbotEnabled then
        local closestPlayer = nil
        local closestDistance = math.huge

        -- Find the closest player
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and (not TeamCheckEnabled or player.Team ~= LocalPlayer.Team) and player.Character and player.Character:FindFirstChild("Head") then
                local targetPosition = player.Character.Head.Position
                local distance = (Camera.CFrame.Position - targetPosition).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end

        -- If a target is found and the aim key is pressed
        if closestPlayer and UserInputService:IsMouseButtonPressed(AimKey) then
            -- Targeting the player's head position
            local targetHeadPosition = closestPlayer.Character.Head.Position

            -- Calculate the direction to the target's head
            local targetDirection = (targetHeadPosition - Camera.CFrame.Position).unit

            -- Smoothly move the camera towards the target head
            local targetCFrame = CFrame.new(Camera.CFrame.Position, targetHeadPosition)
            local smoothFactor = AimSmoothing / 10
            Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, smoothFactor)
        end
    end
end

-- ESP Function
local function ESP()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            -- Create ESP Box
            if not espObjects[player] then
                local espBox = Instance.new("BillboardGui")
                espBox.Adornee = player.Character.Head
                espBox.Size = UDim2.new(0, 50, 0, 50)
                espBox.StudsOffset = Vector3.new(0, 1, 0)
                espBox.AlwaysOnTop = true
                espBox.Parent = player.Character.Head

                local espLabel = Instance.new("TextLabel")
                espLabel.Size = UDim2.new(1, 0, 1, 0)
                espLabel.BackgroundTransparency = 1
                espLabel.Text = player.Name
                espLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
                espLabel.TextStrokeTransparency = 0.8
                espLabel.TextScaled = true
                espLabel.Parent = espBox

                espObjects[player] = espBox
            end
        elseif espObjects[player] then
            espObjects[player]:Destroy()
            espObjects[player] = nil
        end
    end
end

-- Update functions every frame
RunService.RenderStepped:Connect(function()
    if ESPEnabled then
        ESP()
    end
    Aimbot()
end)
